1. If we have a favicon_url but it is invalid we still need to show something. how?
2. Changed BookmarkSchema to have snake_case instead of camel because of how I get the data from the DB. Check errors and also study how is that managed because in the server, with the linter, I'm forced to use CamelCase